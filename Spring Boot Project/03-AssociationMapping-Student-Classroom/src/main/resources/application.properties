spring.application.name=03-AssociationMapping-Student-Classroom
# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/studentdb
spring.datasource.username=root
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


#CREATE TABLE classroom (
#id BIGINT PRIMARY KEY AUTO_INCREMENT,
#name VARCHAR(100) NOT NULL
#);
#
#CREATE TABLE student (
#id BIGINT PRIMARY KEY AUTO_INCREMENT,
#name VARCHAR(100) NOT NULL,
#email VARCHAR(150),
#classroom_id BIGINT,
#
#CONSTRAINT fk_student_classroom
#FOREIGN KEY (classroom_id)
#REFERENCES classroom(id)
#);

#INSERT INTO classroom (name)
#VALUES
#('Java Batch'),
#('Spring Boot Batch');
#
#INSERT INTO student (name, email, classroom_id)
#VALUES
#('Amit', 'amit@gmail.com', 1),
#('Rahul', 'rahul@gmail.com', 1),
#('Vijay', 'vijay@gmail.com', 1),
#('Rohit', 'rohit@gmail.com', 2),
#('Ankit', 'ankit@gmail.com', 2);

#http://localhost:8080/classrooms